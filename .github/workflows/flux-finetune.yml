name: Build FLUX Finetune Image

on:
  push:
    branches: [main]
    paths:
      - 'diffusion/finetune/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: ./diffusion/finetune
          push: true
          tags: |
            ghcr.io/ironfistofgod/flux-finetune:latest
            ghcr.io/ironfistofgod/flux-finetune:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max