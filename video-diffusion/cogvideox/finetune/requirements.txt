torch>=2.3.0
torchvision>=0.18.0

# diffusers — needs to be installed from source for CogVideoX training utils
git+https://github.com/huggingface/diffusers.git

transformers>=4.44.2
accelerate>=0.33.0
peft>=0.12.0
huggingface_hub>=0.23.4

# Prodigy optimizer
prodigyopt>=1.0

# Video decoding
decord>=0.6.0

# Progress bars + experiment tracking
tqdm
wandb

# Serialization
safetensors>=0.4.3
