apiVersion: v1
kind: Service
metadata:
  name: flux-serve
  labels:
    app: flux-serve
  annotations:
    cloud.google.com/backend-config: '{"default": "flux-serve-backend-config"}'
spec:
  type: ClusterIP
  selector:
    app: flux-serve
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
